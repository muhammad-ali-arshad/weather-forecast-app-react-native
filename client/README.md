# Client — Weather Forecast App

React Native (Expo) application for the Weather Forecast project.

**See the [root README](../README.md)** for full documentation, tech stack, and setup (API key, scripts, structure).

## Quick start

```bash
npm install
```

Create `.env` with:

```
EXPO_PUBLIC_WEATHER_API=your_weatherapi_key
```

Then:

```bash
npx expo start
```
